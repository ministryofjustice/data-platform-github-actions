---
name: pre-commit Updater

on:
  schedule:
    - cron: "0 9 * * 1-5" # Monday-Friday at 09:00 UTC
  workflow_dispatch:

permissions: {}

jobs:
  pre-commit-updater:
    name: pre-commit Updater
    permissions:
      contents: write
      pull-requests: write
    uses: ./.github/workflows/pre-commit-updater.yml
